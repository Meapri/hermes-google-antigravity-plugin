diff --git a/agent/agent_runtime_helpers.py b/agent/agent_runtime_helpers.py
index 18ed3102c..4d67b38ce 100644
--- a/agent/agent_runtime_helpers.py
+++ b/agent/agent_runtime_helpers.py
@@ -1574,6 +1574,14 @@ def create_openai_client(agent, client_kwargs: dict, *, reason: str, shared: boo
             agent._client_log_context(),
         )
         return client
+    if agent.provider in ("google-antigravity", "antigravity"):
+        from agent.antigravity_native_adapter import AntigravityChatClient, is_antigravity_base_url
+        _agb = str(client_kwargs.get("base_url", "") or "")
+        if is_antigravity_base_url(_agb):
+            _ags = {k: v for k, v in client_kwargs.items() if k in {"api_key","base_url","default_headers","timeout","http_client"}}
+            client = AntigravityChatClient(**_ags)
+            _ra().logger.info("Antigravity native client created (%s, shared=%s) %s", reason, shared, agent._client_log_context())
+            return client
     if agent.provider == "gemini":
         from agent.gemini_native_adapter import GeminiNativeClient, is_native_gemini_base_url
 
diff --git a/agent/auxiliary_client.py b/agent/auxiliary_client.py
index 64768963e..dac0a2103 100644
--- a/agent/auxiliary_client.py
+++ b/agent/auxiliary_client.py
@@ -360,6 +360,14 @@ def _fixed_temperature_for_model(
     if _is_kimi_model(model):
         logger.debug("Omitting temperature for Kimi model %r (server-managed)", model)
         return OMIT_TEMPERATURE
+    try:
+        from agent.anthropic_adapter import _forbids_sampling_params
+
+        if _forbids_sampling_params(model or ""):
+            logger.debug("Omitting temperature for Claude model %r (provider-managed)", model)
+            return OMIT_TEMPERATURE
+    except Exception:
+        pass
     if _is_arcee_trinity_thinking(model):
         return 0.5
     return None
@@ -4125,6 +4133,29 @@ def resolve_provider_client(
             client_obj, final_model_str, api_key_str, base_url_str, api_mode,
         )
 
+    # ── Google Antigravity (Cloud Code OAuth) ────────────────────────
+    # OAuth-external provider: the token lives in the antigravity oauth file
+    # (auto-refreshed).  Return the native shim, which speaks
+    # chat.completions.create() over the cloudcode-pa generateContent API and
+    # exposes .api_key/.base_url so the main path (agent_init) can rebuild it
+    # through create_openai_client (where the is_antigravity_base_url hook
+    # re-instantiates the shim).  Placed before every generic fallback so an
+    # explicit antigravity choice never silently routes through OpenRouter.
+    if provider in ("google-antigravity", "antigravity"):
+        try:
+            from agent.antigravity_oauth import resolve_antigravity_runtime_credentials
+            from agent.antigravity_native_adapter import AntigravityChatClient
+            creds = resolve_antigravity_runtime_credentials()
+        except Exception as exc:
+            logger.debug("Antigravity credential resolution failed: %s", exc)
+            return None, None
+        if not (creds and creds.get("api_key")):
+            return None, None
+        _ag_client = AntigravityChatClient(api_key=creds["api_key"], base_url=creds["base_url"])
+        _ag_model = model or "gemini-3.5-flash-high"
+        return (_to_async_client(_ag_client, _ag_model, is_vision=is_vision) if async_mode
+                else (_ag_client, _ag_model))
+
     # ── Auto: try all providers in priority order ────────────────────
     if provider == "auto":
         client, resolved = _resolve_auto(main_runtime=main_runtime, task=task)
diff --git a/hermes_cli/auth.py b/hermes_cli/auth.py
index 3547bb873..ea73f9129 100644
--- a/hermes_cli/auth.py
+++ b/hermes_cli/auth.py
@@ -169,6 +169,12 @@ class ProviderConfig:
 
 
 PROVIDER_REGISTRY: Dict[str, ProviderConfig] = {
+    "google-antigravity": ProviderConfig(
+        id="google-antigravity",
+        name="Google Antigravity (Cloud Code)",
+        auth_type="oauth_external",
+        inference_base_url="https://cloudcode-pa.googleapis.com",
+    ),
     "nous": ProviderConfig(
         id="nous",
         name="Nous Portal",
diff --git a/hermes_cli/auth_commands.py b/hermes_cli/auth_commands.py
index 70a346e3e..faff708bc 100644
--- a/hermes_cli/auth_commands.py
+++ b/hermes_cli/auth_commands.py
@@ -34,7 +34,7 @@ from hermes_cli.secret_prompt import masked_secret_prompt
 
 
 # Providers that support OAuth login in addition to API keys.
-_OAUTH_CAPABLE_PROVIDERS = {"anthropic", "nous", "openai-codex", "xai-oauth", "qwen-oauth", "minimax-oauth"}
+_OAUTH_CAPABLE_PROVIDERS = {"anthropic", "nous", "openai-codex", "xai-oauth", "qwen-oauth", "minimax-oauth", "google-antigravity"}
 
 
 def _get_custom_provider_names() -> list:
@@ -409,6 +409,28 @@ def auth_add_command(args) -> None:
         print(f'Added {provider} OAuth credential #{len(pool.entries())}: "{entry.label}"')
         return
 
+    if provider == "google-antigravity":
+        from agent.antigravity_oauth import run_antigravity_login
+        creds = run_antigravity_login()
+        label = (getattr(args, "label", None) or "").strip() or label_from_token(
+            creds["access_token"],
+            _oauth_default_label(provider, len(pool.entries()) + 1),
+        )
+        entry = PooledCredential(
+            provider=provider,
+            id=uuid.uuid4().hex[:6],
+            label=label,
+            auth_type=AUTH_TYPE_OAUTH,
+            priority=0,
+            source=f"{SOURCE_MANUAL}:antigravity_oauth",
+            access_token=creds["access_token"],
+            refresh_token=creds.get("refresh_token"),
+            base_url="https://cloudcode-pa.googleapis.com",
+        )
+        pool.add_entry(entry)
+        print(f'Added {provider} OAuth credential #{len(pool.entries())}: "{entry.label}"')
+        return
+
     raise SystemExit(f"`hermes auth add {provider}` is not implemented for auth type {requested_type} yet.")
 
 
diff --git a/hermes_cli/main.py b/hermes_cli/main.py
index aedcb603e..1d80fce56 100644
--- a/hermes_cli/main.py
+++ b/hermes_cli/main.py
@@ -607,6 +607,7 @@ from hermes_cli.model_setup_flows import (
     _model_flow_xai_oauth,
     _model_flow_qwen_oauth,
     _model_flow_minimax_oauth,
+    _model_flow_google_antigravity,
     _model_flow_custom,
     _model_flow_azure_foundry,
     _model_flow_named_custom,
@@ -3082,6 +3083,8 @@ def select_provider_and_model(args=None):
         _model_flow_qwen_oauth(config, current_model)
     elif selected_provider == "minimax-oauth":
         _model_flow_minimax_oauth(config, current_model, args=args)
+    elif selected_provider in ("google-antigravity", "antigravity"):
+        _model_flow_google_antigravity(config, current_model, args=args)
     elif selected_provider == "copilot-acp":
         _model_flow_copilot_acp(config, current_model)
     elif selected_provider == "copilot":
diff --git a/hermes_cli/model_setup_flows.py b/hermes_cli/model_setup_flows.py
index 312677dab..7959aeda0 100644
--- a/hermes_cli/model_setup_flows.py
+++ b/hermes_cli/model_setup_flows.py
@@ -724,6 +724,66 @@ def _model_flow_minimax_oauth(config, current_model="", args=None):
     print(f"\u2713 Using MiniMax model: {selected}")
 
 
+
+def _model_flow_google_antigravity(config, current_model="", args=None):
+    """Google Antigravity (Cloud Code) OAuth provider: ensure logged in via
+    the Google account, then pick a model. Mirrors the other OAuth flows —
+    the token lives in the antigravity oauth file (auto-refreshed); runtime
+    resolution short-circuits to it in resolve_runtime_provider."""
+    from hermes_cli.auth import (
+        _prompt_model_selection,
+        _save_model_choice,
+        _update_config_for_provider,
+        AuthError,
+        format_auth_error,
+    )
+    from agent.antigravity_oauth import (
+        resolve_antigravity_runtime_credentials,
+        run_antigravity_login,
+    )
+
+    have = False
+    try:
+        have = bool(resolve_antigravity_runtime_credentials().get("api_key"))
+    except Exception:
+        have = False
+    if not have:
+        print("Not logged into Google Antigravity. Starting Google account login...")
+        print()
+        try:
+            run_antigravity_login()
+        except SystemExit:
+            print("Login cancelled or failed.")
+            return
+        except Exception as exc:
+            print(f"Login failed: {exc}")
+            return
+
+    try:
+        creds = resolve_antigravity_runtime_credentials()
+    except AuthError as exc:
+        print(format_auth_error(exc))
+        return
+    except Exception as exc:
+        print(f"Could not resolve Antigravity credentials: {exc}")
+        return
+
+    from hermes_cli.models import _PROVIDER_MODELS
+
+    model_ids = _PROVIDER_MODELS.get("google-antigravity", [])
+    selected = _prompt_model_selection(
+        model_ids,
+        current_model,
+        confirm_provider="google-antigravity",
+        confirm_base_url=creds["base_url"],
+    )
+    if not selected:
+        return
+    _save_model_choice(selected)
+    _update_config_for_provider("google-antigravity", creds["base_url"])
+    print("✓ Using Google Antigravity model: " + str(selected))
+
+
 def _model_flow_custom(config):
     """Custom endpoint: collect URL, API key, and model name.
 
diff --git a/hermes_cli/models.py b/hermes_cli/models.py
index b9b5fc699..18b6185df 100644
--- a/hermes_cli/models.py
+++ b/hermes_cli/models.py
@@ -341,6 +341,18 @@ _PROVIDER_MODELS: dict[str, list[str]] = {
         "MiniMax-M2.1",
         "MiniMax-M2",
     ],
+    "google-antigravity": [
+        "gemini-3.1-pro-high",
+        "gemini-3.1-pro-low",
+        "gemini-3.5-flash-high",
+        "gemini-3.5-flash-medium",
+        "gemini-3.5-flash-low",
+        "gemini-3-flash-high",
+        "gemini-3-flash-low",
+        "claude-opus-4-6-thinking",
+        "claude-sonnet-4-6-thinking",
+        "gpt-oss-120b",
+    ],
     "anthropic": [
         "claude-fable-5",
         "claude-opus-4-8",
@@ -1046,6 +1058,7 @@ CANONICAL_PROVIDERS: list[ProviderEntry] = [
     ProviderEntry("huggingface",    "Hugging Face",             "Hugging Face Inference Providers"),
     ProviderEntry("gemini",         "Google AI Studio",         "Google AI Studio (Native Gemini API)"),
     ProviderEntry("vertex",         "Google Vertex AI",         "Google Vertex AI (Gemini via GCP; OAuth2 service account or ADC, GCP billing/quotas)"),
+    ProviderEntry("google-antigravity", "Google Antigravity",   "Google Antigravity (Cloud Code: Gemini/Claude/GPT-OSS via Google account OAuth login)"),
     ProviderEntry("deepseek",       "DeepSeek",                 "DeepSeek (V3, R1, coder, direct API)"),
     ProviderEntry("xai",            "xAI",                      "xAI Grok (Direct API)"),
     ProviderEntry("zai",            "Z.AI / GLM",               "Z.AI / GLM (Zhipu direct API)"),
diff --git a/hermes_cli/providers.py b/hermes_cli/providers.py
index 0c2a45183..7d13f41fa 100644
--- a/hermes_cli/providers.py
+++ b/hermes_cli/providers.py
@@ -44,6 +44,11 @@ class HermesOverlay:
 
 
 HERMES_OVERLAYS: Dict[str, HermesOverlay] = {
+    "google-antigravity": HermesOverlay(
+        transport="openai_chat",
+        auth_type="oauth_external",
+        base_url_override="https://cloudcode-pa.googleapis.com",
+    ),
     "moa": HermesOverlay(
         transport="openai_chat",
         auth_type="virtual",
diff --git a/hermes_cli/runtime_provider.py b/hermes_cli/runtime_provider.py
index ddc7ccecd..3403d4b95 100644
--- a/hermes_cli/runtime_provider.py
+++ b/hermes_cli/runtime_provider.py
@@ -1603,6 +1603,37 @@ def resolve_runtime_provider(
             "requested_provider": requested_provider,
         }
 
+    # Google Antigravity: OAuth-external provider (Gemini/Claude/GPT-OSS via the
+    # Cloud Code generateContent API). Like Vertex, resolve BEFORE the custom /
+    # credential-pool / generic paths — the token lives in the antigravity oauth
+    # file (auto-refreshed by the vendored client) and must never be treated as a
+    # static api_key by the generic resolver. The returned base_url routes through
+    # the native antigravity shim (is_antigravity_base_url) in create_openai_client.
+    if requested_provider in ("google-antigravity", "antigravity"):
+        from agent.antigravity_oauth import resolve_antigravity_runtime_credentials
+        try:
+            _ag_creds = resolve_antigravity_runtime_credentials()
+        except Exception as exc:
+            raise AuthError(
+                "Google Antigravity credentials could not be resolved. Run "
+                "'hermes auth add google-antigravity' to sign in with your Google "
+                f"account. ({exc})"
+            )
+        _ag_token = str(_ag_creds.get("api_key") or "")
+        if not _ag_token:
+            raise AuthError(
+                "Google Antigravity: no access token found. Run "
+                "'hermes auth add google-antigravity' to sign in."
+            )
+        return {
+            "provider": "google-antigravity",
+            "api_mode": "chat_completions",
+            "base_url": str(_ag_creds.get("base_url") or "").rstrip("/"),
+            "api_key": _ag_token,
+            "source": "antigravity-oauth",
+            "requested_provider": requested_provider,
+        }
+
     custom_runtime = _resolve_named_custom_runtime(
         requested_provider=requested_provider,
         explicit_api_key=explicit_api_key,
